<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>닉네임 소속 검색</title>
  <style>
    body {
      font-family: Pretendard, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      background: #fafafa;
      padding: 40px;
      line-height: 1.5;
    }
    h1 { font-size: 24px; margin: 0 0 8px; }
    p { margin: 0 0 16px; color: #444; }
    .row { display: flex; gap: 8px; flex-wrap: wrap; align-items: center; }
    input {
      padding: 12px 12px;
      width: min(360px, 90vw);
      font-size: 16px;
      border: 1px solid #ddd;
      border-radius: 12px;
      outline: none;
      background: #fff;
    }
    input:focus { border-color: #bbb; }
    button {
      padding: 12px 16px;
      font-size: 16px;
      border: 0;
      border-radius: 12px;
      cursor: pointer;
      background: #111;
      color: #fff;
    }
    button:active { transform: translateY(1px); }
    .card {
      margin-top: 20px;
      padding: 16px;
      border-radius: 16px;
      background: #fff;
      border: 1px solid #eee;
      box-shadow: 0 6px 20px rgba(0,0,0,0.04);
      width: min(560px, 92vw);
    }
    .result { font-size: 18px; }
    .molang { color: #f2b705; font-weight: 800; }
    .meolang { color: #ff7a18; font-weight: 800; }
    .hint { margin-top: 8px; font-size: 13px; color: #777; }
    .count { margin-top: 10px; font-size: 13px; color: #777; }
    code { background: #f4f4f4; padding: 2px 6px; border-radius: 6px; }
  </style>
</head>
<body>

  <h1>닉네임 소속 검색</h1>
  <p>닉네임을 입력하면 어느 소속인지 알려줘요 👀</p>

  <div class="row">
    <input type="text" id="nickname" placeholder="닉네임 입력 (정확히)" autocomplete="off" />
    <button id="btn">검색</button>
  </div>

  <div class="card">
    <div class="result" id="result">닉네임을 입력하고 <b>검색</b>을 눌러줘!</div>
    <div class="hint">예: <code>두광행님</code> / <code>똥신궁</code> / <code>야스오부시</code></div>
    <div class="count" id="count"></div>
  </div>

<script>
  const data = {
    "💛몰랑💛": [
      "미미주인","똥신궁","빠동동","떵구멍","똥안나","두광행님","복학하는화석",
      "돼zl똥","먐무","겨울몽","꼬미집사야","쀄엑","매화려인","딩듀공쥬",
      "그린빌리지","Liber","쭈뿡뿡","이오영","한우리독서","똥서혜","똥쉐도우",
      "똥깐내","뚕호두","별삧 부캐","뺑욱 부캐","옆집누나뒷태","잭쮸","떠블D","밸뱃"
    ],
    "🧡멀랑🧡": [
      "야스오부시","일죽냉동","격천","파높","최강다저스","똥깜내","똥보마",
      "똥추추","똥퉁만","똥겨울이","똥설화","똥오청","심궁우","똥깡내",
      "박국건","똥신궁사생팬","똥미미","갓뀨","똥블D","뚕블D","미미영어닉",
      "무병구(새로 들어올거임)","신지호남편(새로 들어올거임)"
    ]
  };

  const inputEl = document.getElementById("nickname");
  const resultEl = document.getElementById("result");
  const countEl = document.getElementById("count");
  const btnEl = document.getElementById("btn");

  function renderCounts() {
    const molang = data["💛몰랑💛"].length;
    const meolang = data["🧡멀랑🧡"].length;
    countEl.innerHTML = `등록 인원: <span class="molang">💛몰랑💛 ${molang}명</span> · <span class="meolang">🧡멀랑🧡 ${meolang}명</span>`;
  }

  function searchNickname() {
    const input = inputEl.value.trim();

    if (!input) {
      resultEl.innerText = "닉네임을 입력해줘!";
      return;
    }

    for (const group in data) {
      if (data[group].includes(input)) {
        const cls = group.includes("몰랑") ? "molang" : "meolang";
        resultEl.innerHTML = `✅ <strong>${escapeHtml(input)}</strong> 닉네임은 <span class="${cls}">${group}</span> 소속이야!`;
        return;
      }
    }

    resultEl.innerHTML = `❌ <strong>${escapeHtml(input)}</strong> 는 아직 등록되지 않았어.`;
  }

  function escapeHtml(str) {
    return str
      .replaceAll("&", "&amp;")
      .replaceAll("<", "&lt;")
      .replaceAll(">", "&gt;")
      .replaceAll("\"", "&quot;")
      .replaceAll("'", "&#039;");
  }

  btnEl.addEventListener("click", searchNickname);
  inputEl.addEventListener("keydown", (e) => {
    if (e.key === "Enter") searchNickname();
  });

  renderCounts();
</script>

</body>
</html>
